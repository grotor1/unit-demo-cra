on:
  push:
    tags:
      - v[0-9]*
    branches:
      - '/releases/v[0-9]*'

jobs:
  run-tests-on-release:
    uses: ./.github/workflows/test.yaml
  create-release-branch:
    runs-on: ubuntu-latest
    steps:
      - name: "Create release branch"
        if: ${{ github.ref_type == 'tag' }}
        run: echo ${{ github.ref_type == 'tag' }}
